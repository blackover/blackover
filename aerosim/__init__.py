"""AeroSim Lab -- a data-driven flight simulator with a chase-view game front end.

Aircraft behaviour comes from version-controlled YAML data packages rather than
from code, the simulation clock is deterministic and fixed-step, and the
renderer is strictly a viewer: it reads the state and feeds nothing back.
"""

__version__ = "1.0.0"
